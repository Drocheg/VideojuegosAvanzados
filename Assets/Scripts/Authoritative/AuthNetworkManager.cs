using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AuthNetworkManager : MonoBehaviour {
	// // Use this for initialization
	// void Start () {
	// 	_unreliableChannel = NetworkApi.Register(NetworkApi.ChannelType.UNRELIABLE);
		
	// }
	
	// // Update is called once per frame
	// void Update () {
	// }

	// public void SendUnraliable(INetworkEvent event) {
	// 	NetworkAPI.Send(_unreliableChannel, event);
	// }
}
